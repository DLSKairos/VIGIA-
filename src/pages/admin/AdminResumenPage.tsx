import PagePlaceholder from '../PagePlaceholder'

/** Resumen admin (opcional, spec sección 10.2): tarjetas rápidas + centro
 * de notificaciones. Lo construye frontend-dev en fase 3. */
export default function AdminResumenPage() {
  return (
    <PagePlaceholder
      titulo="Resumen admin (/admin)"
      descripcion="Tarjetas rápidas + centro de notificaciones."
    />
  )
}
