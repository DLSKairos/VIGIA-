import PagePlaceholder from '../../PagePlaceholder'

/** Registro de trabajador en vivo — feature estrella del demo (spec sección 11). */
export default function TrabajadorNuevoPage() {
  return (
    <PagePlaceholder
      titulo="Registrar trabajador"
      descripcion="Asistente por pasos: datos básicos -> cargo/taladro -> certificaciones requeridas -> resumen."
    />
  )
}
