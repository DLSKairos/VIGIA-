import PagePlaceholder from '../../PagePlaceholder'

/** Parametrización > Certificaciones (catálogo, spec sección 10.2). */
export default function CertificacionesPage() {
  return (
    <PagePlaceholder
      titulo="Parametrización — Certificaciones"
      descripcion="Catálogo: nombre, tipo (ingreso/permanencia), vigencia en meses."
    />
  )
}
