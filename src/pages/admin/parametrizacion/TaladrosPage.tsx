import PagePlaceholder from '../../PagePlaceholder'

/** Parametrización > Taladros (lista hardcodeada, spec sección 10.2). */
export default function TaladrosPage() {
  return (
    <PagePlaceholder
      titulo="Parametrización — Taladros"
      descripcion="Lista de taladros (Rig 2501 / 3001 / 3003)."
    />
  )
}
