import PagePlaceholder from '../../PagePlaceholder'

/** Parametrización > Cargos (spec sección 10.2). */
export default function CargosPage() {
  return (
    <PagePlaceholder
      titulo="Parametrización — Cargos"
      descripcion="Ver / crear / editar cargos y sus certificaciones requeridas."
    />
  )
}
